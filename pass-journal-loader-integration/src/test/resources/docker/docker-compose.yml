version: '2'

services:

  fcrepo:
    image: oapass/fcrepo:0.1.0@sha256:749cdb6d44ed723f7bd0886a75190f79353078cf4738aa290e5ec1176a9340e4
    environment:
      FCREPO_HOST: "${FCREPO_HOST}"
      FCREPO_PORT: "${FCREPO_PORT}"
    ports:
      - "${FCREPO_PORT}:${FCREPO_PORT}"
    volumes:
      - "mnt/:/mnt"
